DATABASE_URL=postgresql://user:password@host:5432/database
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_replace_me
CLERK_SECRET_KEY=sk_test_replace_me
# Optional for the local terminal agent. Use the exact browser origins that may execute local tasks.
DEVOPS_TERMINAL_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# Optional: otherwise the local agent generates a cryptographically random token on startup.
DEVOPS_TERMINAL_TOKEN=
