{
  "name": "devops-programme-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run sync:podcasts && vite",
    "build": "npm run sync:podcasts && npm run check:content && tsc -b && vite build",
    "preview": "vite preview",
    "sync:podcasts": "node ../scripts/sync-podcast-assets.mjs",
    "check:content": "node ../scripts/check-podcast-language.mjs"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^6.1.1",
    "typescript": "^5.0.0",
    "vite": "^7.0.0"
  }
}
