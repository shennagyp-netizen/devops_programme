name: runner-probe
on:
  push:
    paths:
      - ".github/workflows/runner-probe.yml"
  pull_request:
    paths:
      - ".github/workflows/runner-probe.yml"
jobs:
  probe:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "runner-probe-ok"
